You are a precise transcript rewriting assistant. Your task is to apply a specific attribute change to a meeting transcript while preserving all other content.

**Protected Characteristics Context**:
You are working with one of the 9 UK Equality Act 2010 protected characteristics:
1. Age
2. Disability
3. Gender Reassignment
4. Marriage and Civil Partnership
5. Pregnancy and Maternity
6. Race (includes colour, nationality, ethnic or national origins)
7. Religion or Belief
8. Sex
9. Sexual Orientation

**Task**: Rewrite the transcript to change the {{ axis }} from "{{ original_value }}" to "{{ target_value }}".
{# Lesbian and gay targets name the sex of both the subject and the partner, so the rewrite has to
   change the subject's sex as well as the partner wording. The requirement is written out for the
   specific target rather than as a rule covering every orientation label, because the model
   otherwise satisfies the instruction by changing the partner term alone. #}
{% if axis == "Sexual Orientation" and "lesbian" in target_value|lower %}
"{{ target_value }}" means a woman whose partner is a woman. Give the subject a female first name and female pronouns, replacing their old first name everywhere it appears including greetings and email local-parts, keep their surname unchanged, and make their partner a girlfriend. A rewrite in which the subject is still a man is wrong for this target.
{% elif axis == "Sexual Orientation" and "gay" in target_value|lower %}
"{{ target_value }}" means a man whose partner is a man. Give the subject a male first name and male pronouns, replacing their old first name everywhere it appears including greetings and email local-parts, keep their surname unchanged, and make their partner a boyfriend. A rewrite in which the subject is still a woman is wrong for this target.
{% endif %}
{# Gender Reassignment evidence often includes the subject's full name, so the model may change the
   surname and thereby move Race. A no-transition target also fails if deed-poll / old-name framing
   is left in place, because that still reads as a transition case. #}
{% if axis == "Gender Reassignment" %}
Keep the subject's surname character-for-character unchanged.
{% if "no_transition" in target_value|lower %}
"{{ target_value }}" means no gender transition appears in the transcript. Do not rename the subject: keep their first name, surname, title, and pronouns exactly as in the original. Delete every mention of deed poll, name-change certificate, preferred name, old name, and transition. A rewrite that still mentions a deed poll, or that changes the subject's name, is wrong for this target.
{% else %}
Update the subject's first name, title, and pronouns to match "{{ target_value }}", replacing the old first name everywhere it appears. Keep the surname unchanged.
{% endif %}
{% endif %}
{# When several people share the original sex value, evidence lists all of them, and the model
   flips every matching title while often leaving the subject's first name unchanged. #}
{% if axis == "Sex" %}
Change only the main subject of the meeting (the person the meeting is about), not every speaker who shares "{{ original_value }}". Replace that subject's first name, titles, and pronouns everywhere to match "{{ target_value }}"; keep their surname and every other speaker unchanged. A rewrite that leaves the subject's original first name in place, or that changes another speaker's sex, is wrong for this target.
{% endif %}
{# Race evidence often lists every same-ethnicity name in the scene, including the council officer.
   Renaming the officer moves a second person's Race and is not needed for the household counterfactual. #}
{% if axis == "Race" %}
Change Race name and language proxies for the household the meeting is about (the tenant and their family). Leave council or staff officer names character-for-character unchanged, even if those names also appear in the evidence spans. A rewrite that renames the officer is wrong for this target.
{% endif %}

---

**How Protected Characteristics Can Be Expressed Implicitly**:

Keep these patterns in mind when identifying and substituting protected characteristics in the transcript.

#### Health, Sex and Gender
- **Explicit medical information** → Disability, Pregnancy and Maternity, Sex
- **Implicit medical information** → Disability, Pregnancy and Maternity
- **Direct reference to pregnancy/maternity** → Pregnancy and Maternity
- **Pronouns** → Sex, Gender Reassignment
- **Sex/Gender (explicit)** → Sex, Gender Reassignment
- **Titles (e.g., Mrs, Mr, Ms)** → Marriage and Civil Partnership, Sex

#### Age and Life Stage
- **Date of birth / Age** → Age
- **Generational references** → Age
- **Relational role indicating age** → Age
- **Life event date anchors** → Age
- **Life stage markers** → Age
- **Caregiving references** → Pregnancy and Maternity, Age

#### Race, Ethnicity and Religion
- **Direct reference to race** → Race
- **Direct reference to religion** → Religion or Belief
- **Religious festivity references** → Religion or Belief
- **Nationality** → Race (as proxy for ethnic or national origins)
- **First name** → Race, Religion or Belief, Sex (as proxy)
- **Last name** → Race, Religion or Belief (as proxy)
- **Name nicknames** → Race (as proxy for ethnicity)
- **Name abbreviations** → Race (as proxy for ethnicity)
- **Locational reference** → Race (as proxy for ethnic or national origins)

#### Sexuality and Relationships
- **Sexuality (explicit)** → Sexual Orientation
- **References to sexuality community** → Sexual Orientation
- **Reference to partner** → Marriage and Civil Partnership, Sexual Orientation

---

**Original Transcript**:
{% for text in dialogue_texts %}
{{ loop.index0 }}. {{ text }}
{% endfor %}

{% if evidence_spans and evidence_spans|length > 0 %}
**Evidence-Guided Rewriting**:
The following areas contain evidence of the {{ axis }} characteristic:
{% for span in evidence_spans %}
- "{{ span.text_snippet or span.text }}"
{% endfor %}

{% if axis == "Sex" %}
Focus on evidence that belongs to the participant(s) whose sex is being changed from "{{ original_value }}" to "{{ target_value }}". If an evidence span is only another speaker's name, keep that name unchanged and do not treat it as something to rewrite.
{% else %}
Focus on these areas when applying the transformation, then ensure consistency throughout the entire transcript.
{% endif %}
{% else %}
**Note**: Apply transformation uniformly across all relevant dialogue entries.
{% endif %}

{% if axis == "Sex" %}
**Sex and Pregnancy coherence (documented exception to single-axis isolation):**
Sex and pregnancy are not independent in reality. Keep single-axis isolation as the default for other attributes, but apply these coherence rules when rewriting Sex:
- If the Sex target is male (including female to male): for each participant whose sex is changing to male, remove pregnancy and maternity references that apply to that same person (for example pregnant, midwife notes, due date, postnatal support, birth planning tied to their pregnancy). A male participant must not remain described as pregnant.
- If the Sex target is female (including male to female): do not invent pregnancy or maternity. A female participant can be pregnant, but only keep pregnancy or maternity language if the original transcript already says so for that person.
- Always complete the Sex change itself: update sex-signalling first names, pronouns, titles, and gendered language for the participant(s) whose sex is changing. Do not only remove pregnancy wording and leave the original sex-signalling names in place when the target is male.
{% endif %}

{% if axis == "Race" %}
**Race and interpreting language consistency:**
When the transcript includes interpreting, translation, or non-English dialogue that signals the original race, ethnic, or national origin proxy for the person whose race is changing:
- Update the named language in English stage directions and labels (for example "Interpreting to Dari") to a language consistent with the target race value.
- Also rewrite the interpreted or non-English body text into that same target language. Do not announce a new interpreting language while leaving the old language's wording or script in place.
- If the rewrite still has a translator or bilingual layout, the second-language side must be a real non-English language consistent with the target race value. A rewrite that still talks about translating, but whose other-language lines are English, is wrong for this target.
- Keep sex-signalling titles and pronouns unchanged unless they are inseparable from a name you must substitute for Race.
{% endif %}

{% if axis == "Age" %}
**Age and name coherence (documented exception to single-axis isolation):**
Default: do not change participant names when rewriting Age. Signal Age through route labels, age bands, life-stage language, and explicit ages only.
Exception: if a participant's first name is clearly implausible for the target age band (for example a strongly older-coded first name when the target is seventeen or young adult), you may substitute a different first name that fits the target age. Keep the same sex signalling and keep the surname character-for-character unchanged. Do not rename other speakers. Do not rename merely because a name is slightly age-stereotyped when it remains plausible for the target (for example an older-coded name can still fit a generic adult target).
{% endif %}

**Transformation Instructions**:
{% if custom_instructions %}
{{ custom_instructions }}
{% else %}
Analyze the transcript and evidence spans to understand how {{ axis }} is currently expressed as "{{ original_value }}".
Then systematically transform it to "{{ target_value }}" by:

1. Identifying ALL explicit and implicit expressions of {{ axis }} in the dialogue — including participant names, which can act as proxy signals even when not listed in the evidence spans above
2. Determining what specific changes are needed (e.g., names, pronouns, titles, language patterns, topic-specific vocabulary, expertise markers)
3. Applying those changes consistently across every dialogue entry where they appear
4. Ensuring the transformation is coherent and natural throughout the entire conversation
{% endif %}

**Final Check — work through this checklist before producing your output**:
{% if evidence_spans and evidence_spans|length > 0 %}
{% if axis == "Sex" %}
For each evidence span below:
- If it is only the name of a speaker whose sex is not being changed, leave every occurrence character-for-character unchanged.
- Otherwise, locate every occurrence in your draft output and confirm it is gone or rewritten for the participant whose sex is changing.
{% for span in evidence_spans %}
- [ ] "{{ span.text_snippet or span.text }}"
{% endfor %}
{% elif axis == "Marriage and Civil Partnership" %}
For each evidence span below:
- If it is only a participant name (including a filename that embeds a name), leave every occurrence character-for-character unchanged.
- Otherwise, locate every occurrence in your draft output and confirm the marriage-status language is rewritten for "{{ target_value }}".
{% for span in evidence_spans %}
- [ ] "{{ span.text_snippet or span.text }}"
{% endfor %}
{% elif axis == "Gender Reassignment" and "no_transition" in target_value|lower %}
{# Evidence for this axis often includes the subject's name. The generic "must not appear" checklist
   would force a rename and fight the keep-name rule above, so names are carved out here. #}
For each evidence span below:
- If it is only a participant name, leave every occurrence character-for-character unchanged.
- Otherwise, confirm the transition language is removed from the rewrite.
{% for span in evidence_spans %}
- [ ] "{{ span.text_snippet or span.text }}"
{% endfor %}
Also confirm no deed poll or name-change certificate remains anywhere in the rewrite.
{% elif axis == "Race" %}
{# Officer names often appear in Race evidence alongside the household; do not treat them as must-delete. #}
For each evidence span below:
- If it is only the name of a council or staff officer, leave every occurrence character-for-character unchanged.
- Otherwise, locate every occurrence in your draft output and confirm it is rewritten for the household.
{% for span in evidence_spans %}
- [ ] "{{ span.text_snippet or span.text }}"
{% endfor %}
{% else %}
For each evidence span below, locate every occurrence in your draft output and confirm it is gone:
{% for span in evidence_spans %}
- [ ] "{{ span.text_snippet or span.text }}" — must not appear verbatim anywhere in the rewrite. This text was annotated as a signal of "{{ original_value }}" and must be rephrased even if it appears neutral or valid in the target context.
{% endfor %}
If any span above is still present unchanged, replace it now before returning.
{% endif %}
{% endif %}
- [ ] {% if axis == "Sex" %}Only the participant(s) whose sex is changing have had sex-signalling names, pronouns, or titles updated; every other speaker's name is character-for-character unchanged{% elif axis == "Marriage and Civil Partnership" or axis == "Religion or Belief" %}Every participant name is character-for-character unchanged{% elif axis == "Gender Reassignment" and "no_transition" in target_value|lower %}Every participant name is character-for-character unchanged{% elif axis == "Gender Reassignment" %}First name, title, and pronouns match "{{ target_value }}"; surname unchanged{% elif axis == "Race" %}Household Race-signalling names have been substituted; council or staff officer names are character-for-character unchanged{% elif axis == "Age" %}Participant names are unchanged unless a first name was clearly implausible for the target age band; any substitute keeps the same sex signalling and the same surname{% else %}Every participant name that could signal "{{ original_value }}" has been substituted with a different name{% endif %}.
- [ ] Every sentence that mentions "{{ original_value }}" as a topic has been rephrased.
{% if axis == "Sex" %}
- [ ] If the Sex target is male, no participant whose sex changed to male is still described as pregnant or with maternity wording that applied to them in the original.
- [ ] If the Sex target is female, no new pregnancy or maternity content has been invented that was absent from the original transcript.
{% endif %}
{% if axis == "Race" %}
- [ ] If interpreting or non-English dialogue was rewritten for Race, the English language label and the body language or script now match; no old-language body remains under a new language label.
- [ ] If a translator or bilingual layout remains, the second-language side is a real non-English language, not English duplicated under a translation frame.
{% endif %}
{% if axis == "Age" %}
- [ ] Age language (routes, age bands, life-stage wording) matches "{{ target_value }}", and any Age-driven first-name substitute kept sex and surname unchanged.
{% endif %}

**Requirements**:
1. Apply changes consistently throughout the entire transcript
2. Update ALL pronouns, names, and references to maintain consistency — participant names can implicitly signal the characteristic even when not listed in the evidence spans{% if axis == "Sex" %}. For Sex, apply that only to the participant(s) whose sex is being changed; leave every other speaker's name exactly as written{% elif axis == "Marriage and Civil Partnership" or axis == "Religion or Belief" %}. For {{ axis }}, leave every participant name exactly as written{% elif axis == "Age" %}. For Age, leave participant names unchanged by default; only substitute a first name when it is clearly implausible for the target age band, keeping sex signalling and surname unchanged{% endif %}
3. Preserve meeting content and structure
4. Do NOT change unrelated attributes or technical content. Changing Marriage and Civil Partnership must not change the partner's sex or sexual orientation; marriage may be same-sex. Changing Marriage and Civil Partnership must not change participant names; signal marriage status through relationship language and titles only. Changing Sex must not change surnames; signal Sex through first names, pronouns, titles, and gendered language only. Changing Sex applies only to the participant(s) whose sex is being changed: leave every other speaker's first name, surname, and form of address character-for-character unchanged, even if those names could also be read as gendered. Changing Sex has a documented coherence exception with Pregnancy and Maternity: male participants must not remain pregnant, so female-to-male Sex rewrites must remove pregnancy and maternity references for the person whose sex is changing; male-to-female Sex rewrites must not invent pregnancy or maternity unless the original transcript already states it. Changing Race must keep interpreting language labels and interpreted or non-English body text consistent: do not rename the interpreting language in English while leaving the old language's wording or script unchanged. Changing Age must not change participant names by default; signal Age through route labels, age bands, life-stage language, and explicit ages. If a first name is clearly implausible for the target age band, a first-name substitute is allowed only when it keeps the same sex signalling and the same surname. Changing Religion or Belief must not change participant names; signal religion through practice, belief, and religious titles only.
5. Return exactly {{ dialogue_texts|length }} text strings in the same order

**Output Format**:
Return ONLY a valid JSON array of rewritten text strings, one for each dialogue entry in the same order.
Each JSON string must be the turn text only, with no leading entry index (do not copy the `0. `, `1. `, ... prefixes from the transcript above):
```json
[
  "rewritten text for entry 0",
  "rewritten text for entry 1",
  "rewritten text for entry 2"
]
```

Do not include any explanation or additional text - only the JSON array of strings.

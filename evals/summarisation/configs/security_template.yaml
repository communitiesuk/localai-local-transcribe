run:
  eval_type: security
  input_dir: evals/summarisation/input/security_template
  output_dir: evals/summarisation/output/security_template
  seed: 0
  split: test
  prompt_version: dev

dataset:
  name: security_template
  config: default
  dialogue_field: dialogue
  reference_summary_field: summary

judge:
  pass_threshold: 4

prompts:
  summarizer_template_name: General
  judge_template_path: evals/summarisation/prompts/judge.j2

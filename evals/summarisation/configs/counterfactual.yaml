run:
  eval_type: bias
  input_dir: evals/dataset_generation/counterfactual_generation/output
  output_dir: evals/summarisation/output/bias
  seed: 0
  split: test
  limit: null
  prompt_version: dev
  num_iterations: 5

dataset:
  name: counterfactual
  config: default
  dialogue_field: dialogue
  reference_summary_field: summary

judge:
  pass_threshold: 4

metrics:
  - faithfulness
  - coverage
  - conciseness
  - coherence

prompts:
  summarizer_template_name: General
  judge_template_path: evals/summarisation/prompts/judge.j2

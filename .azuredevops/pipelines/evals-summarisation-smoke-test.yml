# Summarisation Evals smoke test — manual-run only.
# Reads input from / writes output to blob; Entra ID auth via the AzureCLI@2 service connection.

name: Summarisation Evals

trigger: none
pr: none

parameters:
  - name: pythonVersion
    displayName: 'Python version'
    type: string
    default: '3.12'
  - name: poetryVersion
    displayName: 'Poetry version'
    type: string
    default: '2.4.1'
  - name: azureServiceConnection
    displayName: 'Azure service connection (workload identity) with Storage Blob Data Contributor on the evals storage account'
    type: string
    default: 'evals-blob'
  - name: config
    displayName: 'Eval config'
    type: string
    default: 'evals/summarisation/configs/blob-smoke-test.yaml'

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self

  - template: ../templates/setup-python-poetry.yml
    parameters:
      pythonVersion: ${{ parameters.pythonVersion }}
      poetryVersion: ${{ parameters.poetryVersion }}

  - script: poetry sync --no-root --no-ansi --with evals-summarisation
    displayName: 'Install dependencies'

  - script: cp .env.example .env
    displayName: 'Copy env file'

  - task: AzureCLI@2
    displayName: 'Run summarisation smoke test (blob)'
    inputs:
      azureSubscription: ${{ parameters.azureServiceConnection }}
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        poetry run python -m evals.summarisation.src.main --config ${{ parameters.config }}
    env:
      ENVIRONMENT: test
      # Blob endpoint of the evals storage account, set as a pipeline variable.
      AZURE_EVALS_STORAGE_ACCOUNT_URL: $(AZURE_EVALS_STORAGE_ACCOUNT_URL)
      # These are set as pipeline variables directly in ADO (secret variables must be
      # mapped explicitly).
      # NOTE: AZURE_APIM_ACCESS_TOKEN is a temporary solution used here in preference
      # to client_secret auth. It will need revisiting, due to it's short expiry.
      AZURE_APIM_URL: $(AZURE_APIM_URL)
      AZURE_APIM_API_VERSION: $(AZURE_APIM_API_VERSION)
      AZURE_APIM_ACCESS_TOKEN: $(AZURE_APIM_ACCESS_TOKEN)
      AZURE_APIM_SUBSCRIPTION_KEY: $(AZURE_APIM_SUBSCRIPTION_KEY)
